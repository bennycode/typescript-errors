{
  "devDependencies": {
    "@arkweid/lefthook": "^0.7.7",
    "prettier": "^2.8.8",
    "pretty-quick": "^3.1.3"
  },
  "scripts": {
    "fix": "npm run lint --fix",
    "lint": "prettier --ignore-path .gitignore --loglevel error --write \"**/*.{md,yml}\""
  }
}
